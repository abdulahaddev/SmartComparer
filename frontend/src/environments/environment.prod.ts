export const environment = {
  production: true,
  // Change this to your cPanel backend URL once deployed, for example:
  // apiUrl: 'https://api.yourdomain.com' OR apiUrl: '/api' if hosted on the same domain
  apiUrl: 'https://nabatechshop.com/smartcompare_api',
};
